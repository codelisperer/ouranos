# contacts/.env.example --- copy to `.env` (gitignored) and fill in.
#
# These load into the process environment via cons/env:load-dotenv; the host
# environment wins in prod (set real vars there, ship no .env). Add KEY=VALUE
# lines as the project needs them.

# EXAMPLE_API_KEY=REPLACE_ME
